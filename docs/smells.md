# Smell Log

Este ficheiro regista inconsistências, duplicações e riscos com backlog acionável.

| id     | domínio                              | severidade | evidência                                                                                                                                                                                                                         | impacto                                                                                                    | ação                                                                                                                                        | estado    |
| ------ | ------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| SM-001 | Architecture Docs                    | alta       | `docs/ARCHITECTURE_NOTES.md` e outros docs legados contradiziam implementação                                                                                                                                                     | Decisões técnicas com base em documentação obsoleta                                                        | Aplicada policy de deprecação + arquivo + redirecionamento para canónicos                                                                   | resolvido |
| SM-002 | Domain Boundaries                    | alta       | Providers Riverpod estavam em `lib/features/*/domain/providers`                                                                                                                                                                   | Domínio acoplado a framework                                                                               | Providers movidos para `lib/features/*/application/providers`                                                                               | resolvido |
| SM-003 | Money Contract                       | alta       | Existia coexistência de `money` e `*Cents` em fluxos críticos                                                                                                                                                                     | Risco de inconsistência financeira                                                                         | Fallbacks críticos removidos; callables e persistência ativa convergidos para `Money`                                                       | resolvido |
| SM-004 | Trip State Machine                   | alta       | Regras de transição duplicadas em Dart e TS                                                                                                                                                                                       | Drift funcional entre app e backend                                                                        | Criado contrato canónico JSON + script de paridade + gate CI                                                                                | resolvido |
| SM-005 | Quality Gates                        | média      | CI não cobria analyze/test/paridade/functions                                                                                                                                                                                     | Regressões entravam sem bloqueio                                                                           | Criado workflow com gates obrigatórios para app e functions                                                                                 | resolvido |
| SM-006 | Dependencies                         | média      | `flutter_bloc` em `pubspec.yaml` sem uso                                                                                                                                                                                          | Ruído técnico e confusão de stack                                                                          | Dependência removida                                                                                                                        | resolvido |
| SM-007 | Docs Governance                      | média      | Sem policy explícita de deprecação documental                                                                                                                                                                                     | Duplicação de fonte de verdade                                                                             | ADR + policy aplicada e índice oficial atualizado                                                                                           | resolvido |
| SM-008 | Client Eligibility                   | média      | Pré-check cliente e decisão backend não eram equivalentes                                                                                                                                                                         | UX potencialmente inconsistente                                                                            | Pré-check tornou-se informativo e UI passou a usar `LIMIT_EXCEEDED` do backend com details completos                                        | resolvido |
| SM-009 | Testing Baseline                     | média      | `flutter test` falhava por ausência de suíte                                                                                                                                                                                      | Gate CI incompleto                                                                                         | Adicionado smoke test mínimo em `test/smoke_test.dart`                                                                                      | resolvido |
| SM-010 | Legacy Scripts                       | baixa      | Scripts de migração não necessários para app não-prod                                                                                                                                                                             | Superfície de manutenção desnecessária                                                                     | Scripts legados removidos                                                                                                                   | resolvido |
| SM-011 | Legacy Monetary Fields               | média      | `*Cents` ainda visível em validadores, reporting e snapshots ativos                                                                                                                                                               | Dificulta convergência total para contrato money                                                           | Paths ativos migrados para Money-only; fallback legado removido de runtime                                                                  | resolvido |
| SM-012 | Lint Hardening Debt                  | baixa      | Baseline tinha 35 waivers em `analysis_options.yaml`                                                                                                                                                                              | Qualidade de estilo abaixo do target final                                                                 | Onda atual removeu 14 waivers; remanescente: 21/35 (60%)                                                                                    | aberto    |
| SM-013 | Active Trip Drift                    | alta       | Critérios de "viagem ativa" divergiam entre query (`isActive`) e validação em UI/controller (`IsActiveTripState`)                                                                                                                 | Resets de UI e resubscribe thrash em transições adjacentes                                                 | Introduzido `TripPhase` canónico e adoção única em repository/controllers/UI                                                                | resolvido |
| SM-014 | Feature Flag Drift                   | média      | Flags de produto duplicadas em ecrãs (`client_trip_current_screen`, `driver_home_screen`) com `const bool` locais                                                                                                                 | Risco de UX inconsistente entre papéis e ramos mortos                                                      | Criada origem única `AppFeatureFlags` + provider partilhado; branches locais removidos                                                      | resolvido |
| SM-015 | Audit ActionType Drift               | média      | Backend grava múltiplos `audit.actionType` (ex.: `trip_state_transition`, `trip_cancel`, `trip_extension_*`, `trip_payment_retry`) enquanto enum tipado da app cobre apenas `balance_adjustment`, `tariff_edit`, `trip_surcharge` | Classificação incorreta ou perda de semântica na leitura da auditoria administrativa                       | Convergir contrato: alinhar enum/app mapper com universo real de `actionType` do backend ou introduzir fallback explícito por categoria     | aberto    |
| SM-016 | Firestore Schema Compatibility Drift | alta       | Runtime de leitura tolerava documentos legados (coerções bool/string e fallback monetário), contrariando `docs/source_of_truth/*`                                                                                                 | Erros silenciosos e inconsistência de contrato entre app e backend                                         | Validação de leitura tornou-se strict, parsing legado removido, fallback monetário legado removido e propagação explícita de erro de schema | resolvido |
| SM-017 | Trip Timeline Event Duplication      | alta       | `TransitionTripState` gravava `tripEvents` no cliente enquanto o callable backend `transitionTripState` também grava o mesmo evento                                                                                               | Linha temporal do cliente apresentava estados duplicados e perda de clareza operacional                    | Removida gravação duplicada no cliente e deduplicação defensiva de transições adjacentes no consumo                                         | resolvido |
| SM-018 | Trip Contact/Rating Rule Drift       | alta       | Regras Firestore bloqueavam leitura de `trips/{tripId}/driverContactSnapshots/*` e bloqueavam write de rating por cliente em viagem concluída                                                                                     | Erros `permission-denied` em contacto do motorista e submissão de avaliação                                | Regras ajustadas com escopo mínimo: leitura de contacto para participantes e write de rating restrito para cliente em viagem finalizada     | resolvido |
| SM-019 | Driver Contact Snapshot Writer Gap   | alta       | `trips/{tripId}/driverContactSnapshots/{tripId}` era lido pela app mas não havia writer backend nas atribuições/reatribuições                                                                                                     | Cliente ficava sem telefone do motorista e logs de documento inexistente                                   | Backend passou a criar/atualizar snapshot de contacto em atribuição inicial, reatribuição e criação de viagem por reserva                   | resolvido |
| SM-020 | Tariff Source Drift                  | alta       | Edição de tarifa em `tariffs/admin_default` podia divergir de `tariffs/public_default` usado em estimativa e cobrança                                                                                                              | Diferenças entre preço mostrado e preço cobrado em novas viagens                                           | `admin_default` tornou-se fonte única editável e trigger idempotente passou a espelhar automaticamente para `public_default`                | resolvido |
| SM-021 | Mapper Legacy Fallbacks              | média      | Mappers de `users/driverStatus` mantinham defaults silenciosos (`?? false`, role fallback para `client`, parse de timestamp em string, `availabilityEnabled ?? isAvailable`) apesar de schema estrito                             | Dados inválidos podiam ser mascarados quando snapshots sem validação estrita passavam pela camada de dados | Parsing tornado estrito nos mappers e fallback de disponibilidade legado removido do admin runtime                                          | resolvido |
| SM-022 | Forgot Password Stub Drift           | alta       | Ecrã/controller de recuperação existiam, mas `forgot_password_form_controller.dart` usava `Future.delayed` e não chamava `sendPasswordResetEmail`, deixando fluxo incompleto                                                     | Utilizador acreditava que pediu reset sem envio real de email; risco de suporte e falha de UX             | Controller ligado ao use case real, prefill de email no fluxo login, retorno com confirmação genérica e normalização `user-not-found`      | resolvido |
| SM-023 | Recurring DST Drift                  | alta       | Recorrência sem contrato temporal explícito de timezone/hora local arriscava deslocar ocorrências em mudanças de hora                                                                                                              | Reservas semanais podiam ocorrer 1h antes/depois em transições DST                                          | Série passou a guardar `timeZone + localTime + start/end local`; materialização UTC aplica política DST canónica (hora inválida/ambígua)    | resolvido |
| SM-024 | Client-side Bulk Writes for Series   | alta       | Criação/edição/cancelamento de séries recorrentes no cliente implicaria batches volumosos com risco de timeout/partial writes                                                                                                      | Inconsistência de ocorrências e maior fragilidade operacional                                                | Gestão de séries movida para callables backend com idempotência e writes em volume via `BulkWriter`                                         | resolvido |
| SM-025 | Reminder Wide Window Scan            | alta       | Job minutely com varrimento de janela larga forçava leituras desnecessárias e lógica em memória                                                                                                                                    | Custo elevado e degradação com escala                                                                        | Reminder job convergiu para `nextReminderAt` com janela curta `now-2m..now+1m`, claim transacional com lease/reclaim e dedupe no documento | resolvido |
| SM-026 | Admin Tariff Money UX Drift          | média      | Ecrã de tarifários expunha `amountMinor` como “cêntimos” e aceitava input raw, aumentando risco de erro humano e ambiguidade de moeda                                                                                              | Configuração de preços menos previsível e risco de gravação incorreta em edição manual                      | UI migrou para edição monetária formatada por moeda do tarifário, parsing robusto pt-PT sem `double`, validação de moeda única e save bloqueado sem FX | resolvido |
| SM-027 | Legacy `extensionWindow` vs Produto Atual | média | `TripState.extensionWindow` permanece no state machine canónico, mas o produto usa agora `postChargeExtension` pós-cobrança | Risco residual de manutenção de código/strings legadas e confusão em futuros refactors | UX legada neutralizada em `client_trip_current` e fluxo ativo do motorista; manter estado apenas como legado canónico até remoção controlada e continuar limpeza de artefactos remanescentes | mitigado |
| SM-028 | Localization Coverage Outside Audited Surfaces | média | O sweep atual removeu hardcodes PT e estado com strings localizadas nas superfícies auditadas (`manager_home`, `manager_trips`, `client_trips`, `client_reservations`, `reservation_form/pickup/transport`), mas ainda existem áreas fora deste escopo com copy PT em `app_en/app_es` e/ou strings localizadas guardadas em state, por exemplo `driverAvailabilityLocationError`, `driverActiveTripLocationError`, `driver_trips_overview_provider`, `driver_daily_events_provider` e partes de `client_trip_current`/`trip_preview` | Mudar de idioma continua a poder deixar partes da app em português fora das superfícies já cobertas, criando UX inconsistente | Estender o padrão atual (keys ARB reais + typed failures/status codes + tradução só em widgets) aos fluxos de driver, current trip/waiting e restantes controllers/screens com `errorMessage` raw | aberto |
| SM-029 | Tariff Seed/Read Contract Drift | alta | Scripts de seed continuavam a gravar tarifas sem `distanceTiers` e sem o contrato monetário canónico completo, enquanto a app passou a validar e ler `tariffs/public_default` de forma estrita | Ambientes seeded podiam divergir do contrato ativo e bloquear o Trip Preview quando o documento público ficava inválido ou incompleto | Seeds atualizados para payload canónico; leitura do tarifário corrente passou a emitir log crítico e usar fallback operacional para não bloquear pedido imediato | mitigado |
| SM-030 | Admin Transport Type Edit Capability Drift | média | O módulo `admin_transport_types` anunciava “gestão” de tipos de transporte, mas a implementação real suportava apenas `create + list`, sem edição nem propagação do nome atualizado para `vehicles.defaultTransportType.name` | O backoffice aparentava ter funcionalidade de gestão completa, mas o admin não conseguia corrigir catálogo existente e viaturas podiam ficar com nome embebido desatualizado | Edição passou a existir via callable `updateTransportType`, com `id` imutável, resumo explícito do backfill e retry quando a atualização das viaturas fica parcial | resolvido |
| SM-031 | Recoverability Drift in Filtered Empty States | média | Várias listas filtráveis podiam persistir filtros que levavam a zero resultados, mas alguns ecrãs escondiam a `FilterBar` ou deixavam a recuperação enterrada apenas no bottom sheet (`client_reservations`, `manager_trips` e empty states administrativos genéricos) | Utilizador podia ficar preso num empty state sem caminho visível para voltar ao dataset base, sobretudo após reabrir o ecrã com filtros persistidos | `FilterBar` passou a expor `Repor filtros`, os empty states filtrados ganharam CTA `Ajustar filtros`, e os ecrãs críticos mantêm os filtros visíveis mesmo sem resultados | resolvido |
| SM-032 | Audit Identity Drift | média | Registos de auditoria novos gravavam frequentemente `adminName = adminId`, e o ecrã administrativo filtrava/identificava actors por UID técnico em vez de email legível | Operação diária da auditoria exigia interpretar IDs crus, dificultando triagem humana e tornando managers dependentes de contexto técnico | Escritas novas passaram a persistir `adminEmail`, leitura legacy usa resolução best-effort por callable sem leitura direta de `users`, e a UI/filtro passaram a privilegiar email com `adminId` apenas em detalhes técnicos | resolvido |
| SM-033 | Client Dashboard Feature Prematurity | média | O dashboard do cliente expunha `Destinos rápidos` e um atalho `Reservas` concorrente com `Agendar para amanhã`, criando ruído e promovendo uma feature futura fora do fluxo principal | Excesso de opções no landing principal, menor clareza de ação e acesso redundante ao módulo de reservas | Dashboard reduzido a dois atalhos primários (`Minhas viagens`, `Reservar viagem`) e acesso à lista de reservas movido para o próprio fluxo de reserva | resolvido |
| SM-034 | Trip Detail Technical Surface Drift | média | Os detalhes de viagem expunham `Cobrança`, `Detalhes técnicos` e fallback `Referência ...` numa superfície user-facing onde cliente e motorista precisavam sobretudo de leitura simples e recibo exportável | Detalhe de viagem demasiado técnico, maior ruído visual e menor clareza para utilizadores não operacionais | Detalhe do cliente convergiu para `Recibo` com `Exportar PDF`, `Cobrança` foi removida, `Detalhes técnicos` saíram de cliente/motorista e `Participantes` passou a usar fallback `Indisponível` apenas nesta superfície | resolvido |
| SM-035 | Audit Subject Identity Drift | média | O `Alvo` da auditoria ainda caía frequentemente para `subject` técnico (`clientId`) em ajustes de saldo, mesmo quando o fluxo já conhecia `name/email` do utilizador, e registos legacy dependiam de fallback pouco legível | Operação diária da auditoria continuava a exigir interpretação manual de IDs para perceber quem foi afetado por uma ação administrativa | Novos registos passaram a persistir `subjectDisplayName`/`subjectEmail` quando disponíveis, e o histórico usa resolução best-effort por callable em lote + cache local para apresentar `Nome - email` na lista e no detalhe | resolvido |
| SM-036 | Audit Write Validation Drift | média | O `AuditEntryValidator` exigia `adminName` em creates, mas os writers ativos de auditoria (`tariffEdit`, `balanceAdjustment`) persistem `adminEmail` e não garantem `adminName` | Writes válidas de auditoria eram bloqueadas localmente, fazendo ações administrativas parecer falhar apesar do contrato canónico privilegiar `adminEmail` | Validador alinhado com o contrato atual: `adminName` passa a opcional em write e a documentação canónica explicita `adminEmail` como identidade principal do actor | resolvido |
| SM-038 | Client Statement Full-Trip Coupling | média | O extrato do cliente enriquecia `balance_adjustments` lendo o agregado `Trip` completo, ficando exposto a validações e campos legacy irrelevantes para reporting (`supportStatus`, `postChargeExtension.history.requestedMinutes`, etc.) | Um único campo legado fora da superfície do extrato podia degradar ou bloquear o carregamento dos relatórios administrativos | Extrato passou a usar snapshot leve de viagem com apenas os campos apresentados na UI (`driver`, `viatura`, moradas e `receipt.totalMinor`), com fallback/log quando a viagem ligada falta ou não é legível | resolvido |
| SM-037 | Dev Autocomplete Ranking Drift | média | O produto canónico já definia `debug`/`dev` com países `CV + PT`, mas os controllers continuavam a anexar bias de localização local, mantendo QA enviesado para Cabo Verde mesmo em pesquisas multi-país | Ambientes de desenvolvimento pareciam ignorar Portugal, dificultando validação real do catálogo de locais e mascarando a configuração esperada | Política de autocomplete centralizada por ambiente; `debug`/`dev` mantêm `CV + PT` sem `location`/`radius`, enquanto `prod` preserva bias local em `CV` | resolvido |
| SM-039 | Reports Export Semantics Drift | alta | `/admin/reports` misturava resumo agregado, explorador e extrato com exportações dependentes das linhas já carregadas/visíveis, sem metadados completos de proveniência nem lente first-class por viatura | O admin podia gerar ficheiros parciais ou pouco reproduzíveis, fracos para reconciliação, suporte e confronto operacional | O módulo convergiu para um workspace export-first com `Panorama Operacional`, `Extrato do Cliente` e `Extrato do Motorista`; exportação passou a derivar de filtros estruturados + modo ativo, com CSV/XLSX, teto explícito, metadata de proveniência e drill-through para detalhe | resolvido |
| SM-040 | FCM Token Activation Contract Drift | alta | O runtime partilhado de fanout em `functions/src/shared/notifications/fcmFanout.ts` filtrava tokens por `isActive`, enquanto a app, os validators e o contrato Firestore persistem `enabled` em `users/{uid}/fcmTokens/{token}` | Notificações operacionais baseadas no fanout partilhado podiam ser silenciosamente descartadas apesar de existirem tokens válidos | Fanout convergiu para `enabled == true`, alinhado com o contrato canónico de tokens e com os outros senders backend | resolvido |
| SM-041 | Admin Bootstrap Destructive Drift | alta | O único script de bootstrap de admin reutilizável era `seed:reset:basic`, que apagava Firestore, Realtime Database, Storage e Auth antes de recriar o admin | Operação de bootstrap em produção podia destruir o ambiente inteiro por engano | Adicionado `seed:admin` não destrutivo para criar/atualizar apenas o admin em Auth + Firestore, preservando o resto do projeto | resolvido |
| SM-042 | Reservation Docs Drift After Internal-Only Pivot | média | Artefactos não canónicos como `docs/ops/list_filters_matrix.md`, `docs/ops/input_validation_matrix.md`, `docs/adr/0018-recurring-reservations-server-side-reminder-queue.md` e `docs/adr/0024-consistent-list-filtering-pattern.md` ainda referem `clientReservations` e recorrência removida do produto ativo | Leitura parcial da documentação pode reintroduzir pressupostos errados sobre ownership do cliente e reminders/series | Manter `docs/source_of_truth/*` como autoridade, registar o drift e limpar/arquivar estes artefactos quando voltarem a ser tocados | aberto |
| SM-043 | Trip Package Storage Permission Drift | alta | A app fazia upload de fotografias para `tripPackages/{packageId}/...`, mas `storage.rules` só permitia escrita em `users/...` e `vehicles/...`, sem espelhar a claim `manager(tp)` já usada em Firestore | Guardar templates com fotografia falhava com `firebase_storage/unauthorized`, bloqueando criação/edição operacional de pacotes | `storage.rules` alinhadas com o contrato canónico de trip packages, helpers de claims convergidos e testes de regras adicionados para `admin`, `manager(tp)` e acessos negados | resolvido |
| SM-044 | Trip Package Delete Result Naming Drift | baixa | `TripPackageDeleteResult` ainda expõe `cancelledDeparturesCount` e `refundedBookingsCount`, embora o produto ativo já não use `departures` e o delete do template apenas bloqueie novas vendas por defeito | O contrato interno de delete mantém semântica legada e pode confundir manutenção futura apesar de a UI já usar copy correta | Renomear o resultado de delete para refletir o modelo comercial atual ou eliminar o DTO se o backend continuar a devolver apenas sucesso simples | aberto |
| SM-045 | Trip Package Reservation Visibility Drift | alta | `firestore.rules` permitia que `manager` com permissões operacionais lesse `reservations` com `source == 'package'`, contrariando `docs/source_of_truth/admin.md` e o teste canónico `reserva package mantém contrato source-aware sem createdBy e não fica visível para manager` | CI vermelho em `Security Rules CI`/`Quality Gates` e risco de expor reservas de package num domínio que o produto ativo já não trata como workspace operacional de managers | Restringir leitura de `reservations` para staff a `internal_staff`, manter packages fora do domínio operacional ativo e preservar cobertura de teste para evitar regressão | resolvido |
| SM-046 | Admin Trip Package Docs Drift | média | `docs/source_of_truth/admin.md` ainda descrevia `tripPackageDepartures`, ocupação e ações operacionais de saídas, apesar do produto ativo de packages já ser comercial simples (`tripPackages` + `tripPackageBookings`) | Leitura parcial da documentação podia reintroduzir pressupostos errados sobre o modelo ativo e esconder o novo ajuste comercial por transporte | Documentação canónica alinhada ao modelo comercial simples e ao snapshot financeiro autoritativo com `packagePriceMultiplierBasisPoints` exclusivo de packages | resolvido |
| SM-047 | Trip Package Notification Contract Drift | alta | Backend, docs e Flutter divergiam nos eventos de package: checkout já deixara de ser um simples `confirmed`, mas o cliente ainda só reconhecia reminders/cancelamentos legados | Eventos de package podiam ser emitidos com sucesso sem qualquer renderização adequada no cliente/backoffice, ocultando estados críticos do workflow governado | Contrato unificado para `pending_approval`, `approved`, eventos ops e eventos do motorista; mapper/banner/docs alinhados no mesmo change set | resolvido |
| SM-048 | PT-PT Surface Drift | média | A validação manual em emulador ainda expõe superfícies mistas `pt-PT`/inglês em módulos de cliente e admin (`Dashboard`, `Trip packages`, estados de espera e cards administrativos), apesar de o produto declarar `pt-PT` como idioma principal estrutural | A experiência parece inconsistente, reduz confiança do utilizador e mascara regressões de localização em fluxos críticos | Continuar a convergência de strings runtime e labels de módulos para `pt-PT`, priorizando dashboard do cliente, home admin e fluxos de trip/package mais visíveis | aberto |
| SM-049 | Terminal Driver Availability Restore Drift | alta | Logs de `2026-04-28` mostraram viagem `HmcCU33xcdbRIGqAAOpD` em `CHARGE_APPLIED`, mas `driverStatus/NrJHIWpOdqgEt0AWSZkOdJotZt12` permaneceu com `isBusy=true`, `isAvailable=false` e `currentTripId` preso após falha de sync final de metering | Cliente via motoristas indisponíveis apesar de motorista online; motorista ficava preso a viagem terminal | Listener de status passou a limpar ocupação terminal independentemente de `wasActive`, a incluir `updatedAt` no sync de metering e a tratar sync final como best-effort para não bloquear restauração operacional | resolvido |
